"use client";

export default function Navbar() {
  // Navbar is handled inside Layout component (mobile header + desktop sidebar)
  // This component is kept for backward compatibility
  return null;
}
